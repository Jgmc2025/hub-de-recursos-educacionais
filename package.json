{
  "name": "hub",
  "version": "1.0.0",
  "description": "Aplicação Fullstack com Smart Assist (IA) para gerenciamento de materiais didáticos.",
  "main": "index.js",
  "scripts": {
    "server": "cd backend && python -m uvicorn app.main:app --reload",
    "client": "cd frontend && npm run start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "node": "npm install && cd frontend && npm install",
    "env": "cd backend && python -m venv venv && python -m pip install -r requirements.txt && shx cp -n .env.example .env",
    "setup": "npm run env && npm run node",
    "lint": "cd backend && python -m flake8 ."
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "shx": "^0.3.4"
  }
}